let socket = io();
/*let socket = null;

function joinGame() {
    let name = document.forms["join-game"]["name"].value;
    
    if(name == null) {
        alert("Please enter a name");
    }
    else {
        if(socket == null) {
            socket.io();
            socket.on("newuser", addUser);
        }
    }
}*/